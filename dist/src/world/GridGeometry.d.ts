import { Geometry, GridConfiguration } from "..";
export declare class GridGeometry extends Geometry {
    gridConfiguration: GridConfiguration;
    constructor(gridConfiguration: GridConfiguration);
}
